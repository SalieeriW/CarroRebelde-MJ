services:
  server:
    build: ./server
    ports:
      - "8080:8080"

  web:
    build: ./web
    ports:
      - "3000:3000"
    environment:
      - NEXT_PUBLIC_WS_URL=ws://localhost:8080
    depends_on:
      - server
